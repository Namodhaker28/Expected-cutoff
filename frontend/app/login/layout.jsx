export const metadata = {
  title: 'Login',
  description: 'Sign in to Expected Cutoff to track your exam submissions and predictions.',
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
