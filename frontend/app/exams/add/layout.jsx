export const metadata = {
  title: 'Add Exam with AI',
  description:
    'Add a new competitive exam to Expected Cutoff using Gemini AI. Auto-generate subjects, categories, and scoring structure.',
  robots: { index: true, follow: true },
};

export default function AddExamLayout({ children }) {
  return children;
}
