export const metadata = {
  title: 'Dashboard',
  description: 'View your exam submissions and prediction history on Expected Cutoff.',
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
